// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int number;
    
    printf("Enter the Number : ");
    scanf("%d",&number);
    
    for(int i = 2; i <= number; i = i + 2 ){
        if(i % 2 == 0 ){
            printf("%d \n",i);
        }
    }

    return 0;
}

// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int n,sum;
    printf("Enter the value of N: ");
    scanf("%d",&n);
    
    for(;n!=0;n/=10){
        sum+= n%10;
    }
    printf("%d",sum);
    printf("The position of numbers sum is: %d", sum);

    return 0;
}

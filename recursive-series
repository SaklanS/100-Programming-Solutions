// Online C compiler to run C program online
#include <stdio.h>

int series(int n);

int main() {
    int n = 10;
    series(n);

    return 0;
}

int series(int n){
    if (n > 1)
        series(n -1); 
    printf("%d ",n);
}
